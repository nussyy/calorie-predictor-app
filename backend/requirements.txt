# backend/requirements.txt
Flask>=2.0
scikit-learn==1.2.2  # Keep this as it matches the model saving version
numpy==1.26.4     # <-- CHANGE THIS LINE TO THE EXACT VERSION
pandas>=1.3       # Keep pandas flexible for now, pin if needed later
joblib>=1.0
Flask-Cors>=3.0
gunicorn